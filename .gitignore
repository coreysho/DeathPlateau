node_modules/
.DS_Store

server.json

# Your own server address for edit-account.bat. Written on its first run; yours, not the repo's.
edit-account.cfg
content/
engine/
javaclient/
Client-Java/
Engine-TS/
# IDE settings are per machine.
.idea/

# Source caches for asset imports (rev 474, OSRS). Tens of MB each - never commit.
caches/

# Python bytecode, written whenever the tools/models scripts run.
__pycache__/
*.pyc
